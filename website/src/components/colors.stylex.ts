import * as stylex from "@stylexjs/stylex";

/**
 * Semantic demo colors. Each token names what the box is showing, so the same
 * logical names line up across Tailwind / StyleX / Panda demos.
 */
export const colors = stylex.defineVars({
  demoPlain: "#4f46e5",
  demoSquircle: "#db2777",
  demoAmount: "#059669",
  demoCorner: "#7c3aed",
});
